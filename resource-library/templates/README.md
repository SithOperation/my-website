# Generator templates

`generate.py` contains the shared drawing primitives for branded headers, footers, instructions, writing areas, tables, and interactive fields. Resource-specific content is read from `resources/*.json`; the CIA prototype is the first complete definition.
