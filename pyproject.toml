[tool.ruff]
include = [
  "main.py",
  "scripts/demonstrate_x_output_transaction.py",
  "scripts/process_x_dispatch.py",
  "scripts/production_gate.py",
  "scheduling/**/*.py",
  "storage/**/*.py",
  "tests/test_x_output_transaction.py",
]
line-length = 88
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP"]

[tool.mypy]
python_version = "3.13"
strict = true
explicit_package_bases = true
files = [
  "main.py",
  "collectors",
  "models",
  "processors",
  "scheduling",
  "storage",
  "scripts",
  "tests",
]
